services:
  - type: web
    name: task-management-backend
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGODBURL
        sync: false
      - key: JWT_SECRET
        sync: false
      - key: PORT
        value: 10000
